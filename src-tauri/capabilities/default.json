{
  "identifier": "default",
  "description": "Core capabilities for RSSH (all platforms incl. OHOS)",
  "windows": ["main", "rssh-*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-set-always-on-top"
  ]
}
